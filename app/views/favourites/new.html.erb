<h1>New Favourite</h1>

<%= form_tag("/favourites", method: "post") do %>
  <div>
    <%= label_tag(:links, "Links:") %>
    <%= text_area_tag(:links, "", size: "50x20") %>
  </div>
  <div>
    <%= submit_tag("Add") %>
  </div>
<% end %>

<%= link_to 'Back', favourites_path %>
