<%= form_with(model: favourite, local: true) do |form| %>
  <% if favourite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(favourite.errors.count, "error") %> prohibited this favourite from being saved:</h2>

      <ul>
      <% favourite.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :uri %>
    <%= form.text_field :uri, id: :favourite_uri %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :favourite_title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
